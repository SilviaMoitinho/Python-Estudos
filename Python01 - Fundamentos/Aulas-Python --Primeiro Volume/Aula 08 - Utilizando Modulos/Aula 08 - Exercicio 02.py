import random #Essa biblioteca gera numeros aleatorios
num = random.randint(1, 20) #randint gera um numero aleatorio inteiro, e entre parentes podemos escolher de quanto ate quanto ele pode gerar
print (num)